# 聚源数据库数据准确性核查报告 — ashare 项目

> 核查时间：2026-08-03 ｜ 核查方法：使用 gildata-jydb 技能直连 `JYDBTEST / ZBJYDB`（Oracle 11g），
> 复现各脚本的取数与加工逻辑，并与落地产物逐字段比对。
> 数据快照窗口：**2025-10-09 ～ 2026-08-03（最近 200 交易日）**，最新交易日 4591 只活跃 A 股。

## 一、取数口径核查（run_fast_200d_export.py → ma_features_200d_adj.csv）

| 核查项 | 结论 | 说明 |
|---|---|---|
| A 股过滤 | ✅ 正确 | `SUBSTR(SECUCODE,1,2) IN ('60','68','00','30') AND SECUCATEGORY=1`，与技能规范一致；实测 A 股主表 5557 只、最新交易日有行情 4591 只。 |
| 200 交易日窗口 | ✅ 正确 | 以 `INNERCODE=3`（平安银行 000001）最近 200 交易日定界。实测该窗口内全市场不同交易日数 = **200**，INNERCODE=3 自身交易天数 = **200**，无停牌/缺口，窗口定义可靠。 |
| 均线样本下界 | ✅ 合理 | `min_t - 400` 自然日，保证 MA250 历史窗口充足。 |
| 比例复权 | ✅ 正确 | `ADJ_CLOSE = CLOSEPRICE × RATIOADJUSTINGFACTOR`，用 `LAST_VALUE(...) IGNORE NULLS` 按交易日前向填充因子，符合聚源官方比例复权公式。 |
| 双口径均线 | ✅ 正确 | 复权(adj)与未复权(raw)各 6 条均线（MA5/10/20/60/120/250），`ROWS BETWEEN N PRECEDING AND CURRENT ROW` 行级 trailing 窗口，口径正确。 |
| 多头排列 / 标签 | ✅ 正确 | `ALIGN_T0`（MA5>MA10>…>MA250）、`ALL_UP`（全均线上升）、`LABEL`（对齐+上升+`DAY_GAP<=7` 防停牌）定义严谨，无未来数据泄露。 |

## 二、申万行业字典核查（fetch_sw_map.py → sw_industry_map.json）

| 核查项 | 结论 | 说明 |
|---|---|---|
| STANDARD=38 含义 | ✅ 确认 = 申万 2021 | 库内唯一拥有 **31 个一级行业**、且行业名（机械设备/基础化工/医药生物/电子/电力设备…）与申万 2021 版完全吻合的标准。 |
| 所用字段 | ✅ 存在 | `FIRSTINDUSTRYNAME / SECONDINDUSTRYNAME / STANDARD / CANCELDATE / INFOPUBLDATE / COMPANYCODE / ID` 在 `LC_EXGINDUSTRY` 中均存在。 |
| 样本映射 | ✅ 全部一致 | 000333→家用电器/白色家电、600519→食品饮料/白酒Ⅱ、601939→银行/国有大型银行Ⅱ、600919→银行/城商行Ⅱ、002415→计算机/计算机设备、601000→交通运输/航运港口、600000→银行/股份制银行Ⅱ，与 DB 完全一致。 |
| 覆盖率 | ⚠️ 见下 | 全 A 股 4605/5557 = 82.87% 命中；但**全部 4591 只活跃面板股 100% 命中**（未命中 952 只均为无交易的非活跃/退市股，永不进入面板）。显示层零影响。 |

> 附带发现：项目内探查脚本 `probe_lc_exgindustry.py` / `probe_sw_standard.py` 引用的 `INDUSTRYCLASSIFICATIONSYSTEM` 列在库中**不存在**（会报错），但生产脚本 `fetch_sw_map.py` 不依赖它，不受影响。

## 三、切片与聚合核查（convert_200d_to_json.py）

| 核查项 | 结论 | 说明 |
|---|---|---|
| dates_index.json | ✅ 一致 | 200 个交易日的 `total / adj_label1_count / raw_label1_count` 与 CSV 原始计数**逐日 0 不一致**。 |
| stock_200d_summary.json | ✅ 一致 | 双口径连续多头天数 `adj_streak / raw_streak` 对 4613 只股票独立重算**全部吻合**。 |
| 前端契约 | ✅ 一致 | `index.html` 仅通过 `dates_index.json` 索引加载 `data_*.json`，字段名（sw_ind1、adj_streak、adj.ma5、adj_close 等）与 convert 输出完全对应。 |

## 四、端到端精度（独立复算 vs 落地产物）

- **最新交易日全市场矩阵（4591 只）**：直接对 DB 重跑同一套 SQL，与 CSV 比对 → `LABEL / MA5 / MA250 / ADJ_CLOSE / ALIGN_T0` **0 处不一致**；LABEL=1 数 DB=16、CSV=16。
- **样股独立基础表重算（600519/000333/601939/600919/002415/601000）**：用 `QT_DAILYQUOTE` + `QT_ADJUSTINGFACTOR` 从零计算 MA 与复权价，与 CSV **全部精确吻合**。例：茅台 `RAW_MA250=1398.5` vs `ADJ MA250=12085.6`，复权效应显著且计算正确。

## 五、发现的问题与改进建议（按严重度）

1. **【低 · 数据卫生】孤立陈旧切片**
   `data/` 中存在 `data_2025-09-30.json`，**未被** `dates_index.json` 引用（窗口已前移一天）。根因：`convert_200d_to_json.py` 重生成时不清空旧切片。前端不会加载它，**不影响正确性**，仅浪费磁盘。
   → 建议：重生成前 `rm -f data/data_*.json`，或生成后对不在 dates_index 的文件做 prune。

2. **【低 · 边缘】连续天数从全局最新日计数**
   `adj_streak / raw_streak` 从“全局最新交易日”向前数，若某股最新日停牌则 streak 归零。实测 22 只最新日缺勤股的正确 streak 也均为 0，**当前无实际影响**；但建议改为“从各股自身最后交易日”计数更稳健（只可能低估、不会高估）。

3. **【信息】申万未分类**
   952 只 SECUMAIN A 股无活跃申万 2021 分类（标记“未分类”），均为非活跃股，面板 0 影响。convert 已用“未分类”兜底，无需处理。

4. **【提示】SQL 死代码**
   `ma_features_200d_adj.sql` 中 `vd` CTE 未被引用（最终用 `min_max_dates`）。行为等价于 `.py`，不影响结果；若团队改用 `.sql` 直跑，结论同样成立。

## 六、结论

数据流向整体**准确**。取数口径（A 股过滤、200 日窗口、比例复权、双口径均线、多头排列标签）与聚源官方定义一致；加工指标经 DB 独立重算与样股基础表重算**100% 吻合**；切片/聚合产物与上游 CSV 完全一致。仅发现 2 项低风险的数据卫生/边缘问题（均不影响当前正确性），已给出修复建议。
