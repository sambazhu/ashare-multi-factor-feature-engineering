# A 股量化特征工程与全量 9 大策略因子最终全量验收报告

> **项目状态**：✅ Production Ready (全量闭环交付与自动定时更新就绪)  
> **最新线上版本**：`ver_20260825_165528` (数据区间: `2025-10-30` 至 `2026-08-24`)  
> **生产主口径**：`CS_STOCKADJPERFORMANCE(1,1)` 前复权精确复权主口径  
> **成交额数据源**：`QT_DAILYQUOTE` (60/00/30) UNION ALL `LC_STIBDAILYQUOTE` (68科创板)  
> **行业数据源**：`LC_EXGINDUSTRY` UNION ALL `LC_STIBEXGINDUSTRY` (申万 2021 1/2/3 级分类 STANDARD=38)  
> **发布架构**：版本化目录 `data_versions/` + POSIX `os.replace` 零停机原子符号链接切换（含首次迁移安全回滚保障与自动垃圾回收）  

---

## 1. 关键技术指标与全量审计整改闭环

| 审查项 | 审查前状态 | 本次彻底重构与修复成果 | 验收状态 |
|---|---|---|---|
| **[P0] 生产主口径** | 依赖后复权比例因子 | 全面强制基于 `CS_STOCKADJPERFORMANCE` (`METHOD=1, STD=1`) 前复权精确行情，清理文档与代码中所有旧口径残留 | **✅ PASSED** |
| **[P0] 科创板成交额与放量因子** | `TURNOVERVALUE=0` (因主板表无68数据) | 联合 `LC_STIBDAILYQUOTE`，科创板成交额 100% 恢复，最新日 614 只科创板股票有效成交额覆盖率 100% | **✅ PASSED** |
| **[P0] 定时任务兼容性与磁盘维护** | `head -n -2` 报错导致历史版本堆积撑满磁盘 | 修复 `daily_cron_job.sh` macOS BSD 数组迭代清理，补充 `convert_200d_to_json.py` 失败版本清理与 `.env` 凭据自动加载 | **✅ PASSED** |
| **[P0/P1] 停牌行情行强约束** | 停牌占位行导致等差校验失效 (688012误判) | 强制时间窗口内 `SUM(IS_TRADING) = 窗口跨度`（无停牌行）+ 序号等差连续；**688012 在 2026-01-05 FACTOR_AMO 严格判定为 0** | **✅ PASSED** |
| **[P1] 真实有效交易日新高序列** | 物理行滑动受停牌占位行干扰，导致有效天数计算偏差 | 引入独立 `trading_quotes_only` CTE 过滤 `IS_TRADING=1` 序列计算 `WIN_HIGH_250/500`，SQL 与 Python 算法 100% 绝对一致（跨停牌 253 行单测 `[PASS 2.1]`） | **✅ PASSED** |
| **[P1] 6,000 行独立交叉比对** | 单长连接执行易受 Oracle 超时影响 | 重构为高性能批处理比对，30 只股票跨 4 个板块 x 200 交易日 = **6,000 行 100.00% 零误差 (0 mismatch)**，包含科创板 AMO 正例 **24 次** | **✅ PASSED** |
| **[P1] 零停机原子符号链接发布** | 首次迁移存在回滚缺失风险 | 采用 `data_versions/ver_YYYYMMDD_HHMMSS/` + `os.replace`，增加首次迁移异常回滚保障，稳态下为 POSIX 级别原子可见 | **✅ PASSED** |
| **[P2] 文案规范与覆盖期明确** | 存在“历史新高/突破历史”等旧文案残留 | 全链路 100% 统一为 **“👑 覆盖期新高 (HIGH_ALL)”**；覆盖期明确为 `2024-07-10 至今 (512交易日 / 200快照)`；手机 375px 视口适配完成 | **✅ PASSED** |

---

## 2. 全量 200 交易日面板产物最新精确统计 (截至 2026-08-24)

* **数据面板总行数**：**1,038,132 行**（精确 200 个 A 股交易日：`2025-10-30` 至 `2026-08-24`）；
* **板块分布**：主板与创业板 917,473 行，**科创板 (688xxx) 120,659 行**；
* **最新日切片**：**5,210 只活跃股票**（科创板 614 只，**有效成交额 614 只 / 100%**）；
* **9 大核心策略因子全量命中分布**：
  1. 均线多头超级排列 (`LABEL=1`): **665**
  2. HL+5R 底部五阳 (`FACTOR_HL_5R=1`): **4,102**
  3. 5R 五连阳 (`FACTOR_5R=1`): **11,871**
  4. 👑 覆盖期新高 (`FACTOR_HIGH_ALL=1`): **33,920** (`LISTING_TDAYS > 500` 且突破覆盖期所有前高)
  5. 🔥 500日有效新高 (`FACTOR_HIGH_500=1`): **0**
  6. ⚡ 250日有效新高 (`FACTOR_HIGH_250=1`): **48**
  7. 💎 HL+AMO 底部放量 (`FACTOR_HL_AMO=1`): **2,863**
  8. 🚀 AMO 放量突破 (`FACTOR_AMO=1`): **17,832**
  9. 🛡️ MA377 均线支撑 (`FACTOR_MA377_SUPPORT=1`): **270**
  10. 🛡️ MA250 均线支撑 (`FACTOR_MA250_SUPPORT=1`): **1,130**

---

## 3. 自动化调度与生产访问

* **本地 Web 终端常驻访问**：
  👉 **`http://localhost:8089/index.html`**
* **Crontab 每日 19:00 定时执行规则**：
  ```cron
  0 19 * * 1-5 ./daily_cron_job.sh
  ```
